int main() {
    int a, b;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    // Using ternary operator
    (a > b) ? printf("%d is maximum", a) : printf("%d is maximum", b);

    return 0;  }  ```
